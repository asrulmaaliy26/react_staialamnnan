name: 🚀 Auto Deploy - staialmannan.ac.id

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: 🌐 Deploy ke staialmannan.ac.id
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📦 Install Dependencies
        run: npm ci

      - name: 🏗️ Build Project
        run: npm run build

      - name: 📤 Upload dist/ ke VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          source: "dist/*"
          target: "/home/staialmannan/staialmannan.ac.id"
          strip_components: 1

      - name: ♻️ Reload Apache di VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            echo "✅ File berhasil diupload ke /home/staialmannan/staialmannan.ac.id"
            echo "🔧 Memperbaiki Permission (Gembok) dan Menghapus index.php bawaan..."
            chown -R staialmannan:staialmannan /home/staialmannan/staialmannan.ac.id
            echo "♻️ Graceful reload Apache..."
            /usr/local/apps/apache2/bin/apachectl graceful
            echo "🎉 https://staialmannan.ac.id sudah terupdate!"

      - name: ✅ Deploy Berhasil
        if: success()
        run: |
          echo "🎉 Deploy berhasil!"
          echo "🌐 Cek: https://staialmannan.ac.id"

      - name: ❌ Deploy Gagal
        if: failure()
        run: echo "💥 Deploy gagal! Cek log di atas."
